# Name-based PHP routing (Windows php-cgi FastCGI stand-in for php-fpm).
#
# AllowOverride All → project .htaccess enabled (same rules as XAMPP).
#
# XAMPP/mod_php accepts /index.php/controller PATH_INFO. php-cgi does not
# unless SCRIPT_NAME is the real .php path (not …/index.php/controller).
# We rewrite …/*.php/* → …/*.php?/* and force SCRIPT_NAME via FIXED_SN.

ProxyRequests Off
ProxyPreserveHost On
HttpProtocolOptions Unsafe

# localhost / latest — DEFAULT PHP (managed by Stack Panel)
<VirtualHost *:80>
    ServerName localhost
    ServerAlias latest localhost.latest
    DocumentRoot "C:/web/htdocs"

    RewriteEngine On
    # /oe/index.php/login/validation/ → /oe/index.php?/login/validation/ + FIXED_SN=/oe/index.php
    RewriteRule ^/(.*\.php)/(.*)$ /$1?/$2 [E=FIXED_SN:/$1,NE,QSA,L]

    <Directory "C:/web/htdocs">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.php index.html
        AcceptPathInfo On
        <FilesMatch "\.php$">
            SetHandler "proxy:fcgi://127.0.0.1:9080/"
        </FilesMatch>
    </Directory>
    # After [L] rewrite, FIXED_SN becomes REDIRECT_FIXED_SN
    ProxyFCGISetEnvIf "reqenv('REDIRECT_FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:REDIRECT_FIXED_SN}"
    ProxyFCGISetEnvIf "reqenv('FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:FIXED_SN}"
    ProxyFCGISetEnvIf "true" SCRIPT_FILENAME "C:/web/htdocs%{reqenv:SCRIPT_NAME}"
    ErrorLog "logs/php-default-error.log"
    CustomLog "logs/php-default-access.log" "%h %l %u %t \"%r\" %>s %b \"%{Host}i\""
</VirtualHost>

# PHP 7.4
<VirtualHost *:80>
    ServerName php7
    ServerAlias localhost7 localhost.php7
    DocumentRoot "C:/web/htdocs"
    RewriteEngine On
    RewriteRule ^/(.*\.php)/(.*)$ /$1?/$2 [E=FIXED_SN:/$1,NE,QSA,L]
    <Directory "C:/web/htdocs">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.php index.html
        AcceptPathInfo On
        <FilesMatch "\.php$">
            SetHandler "proxy:fcgi://127.0.0.1:9074/"
        </FilesMatch>
    </Directory>
    ProxyFCGISetEnvIf "reqenv('REDIRECT_FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:REDIRECT_FIXED_SN}"
    ProxyFCGISetEnvIf "reqenv('FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:FIXED_SN}"
    ProxyFCGISetEnvIf "true" SCRIPT_FILENAME "C:/web/htdocs%{reqenv:SCRIPT_NAME}"
    ErrorLog "logs/php74-error.log"
    CustomLog "logs/php74-access.log" "%h %l %u %t \"%r\" %>s %b \"%{Host}i\""
</VirtualHost>

# PHP 8.0
<VirtualHost *:80>
    ServerName php8
    ServerAlias localhost8 localhost.php8
    DocumentRoot "C:/web/htdocs"
    RewriteEngine On
    RewriteRule ^/(.*\.php)/(.*)$ /$1?/$2 [E=FIXED_SN:/$1,NE,QSA,L]
    <Directory "C:/web/htdocs">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.php index.html
        AcceptPathInfo On
        <FilesMatch "\.php$">
            SetHandler "proxy:fcgi://127.0.0.1:9080/"
        </FilesMatch>
    </Directory>
    ProxyFCGISetEnvIf "reqenv('REDIRECT_FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:REDIRECT_FIXED_SN}"
    ProxyFCGISetEnvIf "reqenv('FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:FIXED_SN}"
    ProxyFCGISetEnvIf "true" SCRIPT_FILENAME "C:/web/htdocs%{reqenv:SCRIPT_NAME}"
    ErrorLog "logs/php80-error.log"
    CustomLog "logs/php80-access.log" "%h %l %u %t \"%r\" %>s %b \"%{Host}i\""
</VirtualHost>

# PHP 8.4
<VirtualHost *:80>
    ServerName php8.4
    ServerAlias localhost84 localhost.php84
    DocumentRoot "C:/web/htdocs"
    RewriteEngine On
    RewriteRule ^/(.*\.php)/(.*)$ /$1?/$2 [E=FIXED_SN:/$1,NE,QSA,L]
    <Directory "C:/web/htdocs">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.php index.html
        AcceptPathInfo On
        <FilesMatch "\.php$">
            SetHandler "proxy:fcgi://127.0.0.1:9084/"
        </FilesMatch>
    </Directory>
    ProxyFCGISetEnvIf "reqenv('REDIRECT_FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:REDIRECT_FIXED_SN}"
    ProxyFCGISetEnvIf "reqenv('FIXED_SN') =~ m#^/.+#" SCRIPT_NAME "%{reqenv:FIXED_SN}"
    ProxyFCGISetEnvIf "true" SCRIPT_FILENAME "C:/web/htdocs%{reqenv:SCRIPT_NAME}"
    ErrorLog "logs/php84-error.log"
    CustomLog "logs/php84-access.log" "%h %l %u %t \"%r\" %>s %b \"%{Host}i\""
</VirtualHost>

Include conf/extra/httpd-sites.conf
